# レシート撮影・自動家計簿アプリ（Smart Receipt）詳細設計書

## 1. プロジェクト概要
スマートフォンでレシートを撮影し、OCR技術を用いて内容を自動読取・保存するWebアプリケーション。
既存のポートフォリオサイトの一部として機能し、フロントエンド技術のみで動作するセキュアかつ軽量な設計とする。

## 2. システムアーキテクチャ

### 2.1 技術スタック
- **言語**: HTML5, CSS3, JavaScript (ES6+)
- **OCRエンジン**: Tesseract.js (v4.x)
- **グラフ描画**: Chart.js
- **ストレージ**: LocalStorage (IndexedDBへの移行も考慮)
- **デザイン**: CSS Variables (既存ポートフォリオと統一感を持たせる)

### 2.2 ディレクトリ構造
```
my-portfolio/
├── index.html           # ポートフォリオトップ
├── styles.css           # 共通スタイル
└── apps/
    └── receipt-ocr/
        ├── index.html       # アプリ本体
        ├── styles.css       # アプリ専用スタイル
        ├── js/
        │   ├── app.js                # メインロジック・UI制御
        │   ├── ocr-service.js        # OCR処理・画像処理
        │   ├── parser.js             # テキスト解析ロジック
        │   ├── category-classifier.js # カテゴリ自動分類・学習
        │   ├── storage.js            # データ保存管理
        │   └── calendar.js           # カレンダー生成ロジック
        └── assets/
            └── placeholder.png
```

## 3. データモデル設計 (LocalStorage)

### 3.1 ストレージキー

#### `smart_receipt_data_v1` - レシートデータ
JSON形式で配列として保存。

```json
[
  {
    "id": "uuid-v4-string",
    "createdAt": "2024-03-20T10:00:00.000Z",
    "image": "data:image/jpeg;base64,...", // サムネイル用（低解像度）
    "merchant": {
      "name": "セブンイレブン",
      "raw": "ｾﾌﾞﾝ-ｲﾚﾌﾞﾝ 渋谷店",
      "confidence": "high" // 'high' | 'low' | 'failed'
    },
    "date": "2024-03-20",
    "totalAmount": 1250,
    "amountConfidence": "high", // 'high' | 'low' | 'failed'
    "category": {
      "id": "food", // カテゴリID
      "name": "食費", // カテゴリ名（表示用）
      "autoDetected": true // 自動分類か、ユーザー修正か
    },
    "items": [
      { "name": "おにぎり", "price": 150 },
      { "name": "お茶", "price": 160 }
    ],
    "status": "confirmed", // 'pending' | 'confirmed'
    "ocrRawText": "読み取った全文（デバッグ用）"
  }
]
```

#### `smart_receipt_category_learning_v1` - カテゴリ学習データ
店舗名とカテゴリのペアを保存し、次回の自動分類に活用。

```json
{
  "セブンイレブン": "food",
  "マツモトキヨシ": "daily",
  "ガスト": "restaurant",
  "スターバックス": "cafe"
}
```

#### `smart_receipt_settings_v1` - アプリ設定
予算やカテゴリ定義など。

```json
{
  "weeklyBudget": 10000, // 週間予算（円）
  "categories": [
    { "id": "food", "name": "食費", "color": "#3b82f6" },
    { "id": "daily", "name": "日用品", "color": "#10b981" },
    { "id": "restaurant", "name": "外食", "color": "#f59e0b" },
    { "id": "cafe", "name": "カフェ", "color": "#8b5cf6" },
    { "id": "transport", "name": "交通費", "color": "#ef4444" },
    { "id": "other", "name": "その他", "color": "#6b7280" }
  ],
  "categoryKeywords": {
    "food": ["スーパー", "コンビニ", "セブン", "ファミマ", "ローソン"],
    "daily": ["マツキヨ", "薬局", "ドラッグ"],
    "restaurant": ["ガスト", "サイゼ", "レストラン"],
    "cafe": ["スタバ", "タリーズ", "カフェ"],
    "transport": ["JR", "地下鉄", "バス", "タクシー"]
  }
}
```

**信頼度の定義**:
- `high`: キーワード（「合計」など）が見つかり、明確に抽出できた
- `low`: キーワードが見つからず、最大値推測で抽出した（要確認）
- `failed`: 全く読み取れなかった、またはOCR自体が失敗した

> **注意**: LocalStorageの容量制限（約5MB）を考慮し、画像は長辺300px程度にリサイズしたサムネイルのみ保存するか、あるいは画像保存を必須としない設計にする。OCR解析後のテキストデータが主役。

## 4. 機能詳細仕様

### 4.1 撮影・画像処理フェーズ
1. **カメラ起動**: `<input type="file" accept="image/*" capture="environment">`
2. **画像前処理 (Canvas)**:
   - グレースケール化: RGB平均値で置換
   - 二値化: 閾値処理で文字をくっきりさせる
   - ノイズ除去: 簡易的な平滑化フィルタ（オプション）

### 4.2 OCR解析フェーズ (Tesseract.js)
- 言語設定: `jpn` (日本語)
- 認識モード: LSTM (標準モデル)

### 4.3 テキスト解析ロジック (`parser.js`)
OCRの出力（Raw Text）から正規表現で情報を抽出する。

**戻り値の構造**:
```javascript
{
  date: { value: "2024-03-20", confidence: "high" | "low" | "failed" },
  merchant: { name: "セブンイレブン", confidence: "high" | "low" | "failed" },
  amount: { value: 1250, confidence: "high" | "low" | "failed" },
  rawText: "OCRで読み取った全文"
}
```

#### A. 日付抽出ロジック
以下のパターンを優先順位順に検索:
1. `202[0-9]年[0-1]?[0-9]月[0-3]?[0-9]日`
2. `202[0-9]/[0-1]?[0-9]/[0-3]?[0-9]`
3. `R[0-9]\.[0-1]?[0-9]\.[0-3]?[0-9]` (和暦対応)

#### B. 金額抽出ロジック（優先順位付き）
1. **最優先**: 「合計」「小計」「合 計」などのキーワードがある行を探す
   - その行、または次の行にある数値 (`¥`, `,`, `円` を除去) を取得
   - **成功時**: `{ amount: 1250, confidence: 'high' }` を返す
2. **次優先**: レシートの下部20%の領域にある最大数値を探す
   - 日本のレシートは下部に合計金額が集中する傾向を利用
   - ただし、電話番号や会員番号などの定型パターンは除外
   - 見つかった場合: `{ amount: 1250, confidence: 'high' }` を返す（キーワードがないが、位置的に信頼できる）
3. **推測**: キーワードも下部にも見つからない場合、テキスト全体から最大数値を候補として提示
   - 電話番号や会員番号を除く
   - **推測時**: `{ amount: 1250, confidence: 'low' }` を返す
4. **失敗**: 全く数値が見つからない、またはOCRテキストが空の場合
   - **失敗時**: `{ amount: null, confidence: 'failed' }` を返す

#### C. 店舗名抽出ロジック
1. レシート上部 20% の領域にある、フォントサイズが大きい行
2. 「株式会社」「店」を含む行
3. 電話番号 (`0\d{1,4}-\d{1,4}-\d{4}`) を検出し、その周辺の行

#### D. カテゴリ自動分類ロジック (`category-classifier.js`)
1. **学習データ優先**: `smart_receipt_category_learning_v1` に店舗名が登録されている場合、そのカテゴリを最優先で適用
2. **キーワードマッチング**: 学習データにない場合、`categoryKeywords` 設定と照合
   - 店舗名や `rawText` に含まれるキーワードからカテゴリを推測
   - 例: 「セブン」→ `food`, 「マツキヨ」→ `daily`
3. **デフォルト**: どちらも該当しない場合、`other` カテゴリを適用
4. **ユーザー修正の学習**: ユーザーがカテゴリを手動で変更した場合、`店舗名: カテゴリID` のペアを学習データに保存

### 4.4 UI画面仕様

#### 画面1: ダッシュボード (Home)
- **ヘッダー**: アプリ名、戻るボタン、設定ボタン
- **サマリー**: 今月の支出合計
- **週間グラフ (Chart.js)**:
  - 直近1週間（月〜日）の支出棒グラフ
  - **予算ライン**: 週間予算を破線でオーバーレイ表示
  - **色分け**:
    - 予算超過日: 赤色 (`#ef4444`)
    - 予算内: 青色 (`#3b82f6`)
    - 予算の50%以下: 緑色 (`#10b981`)
- **カレンダービュー (自作)**:
  - 月ごとのカレンダーを表示
  - 各日付セルに日次合計金額を表示 (`¥1,200`)
  - 日付クリックでその日のレシート一覧を表示するモーダル/画面へ遷移
- **リスト**: 最近登録したレシート (カード形式、カテゴリ色で識別)
- **FAB (Floating Action Button)**: カメラ起動ボタン

#### 画面2: 解析結果・修正 (Editor)
- **画像プレビュー**: 撮影した画像
- **OCR処理中インジケーター**: 読み取り中のローディング表示
- **フォーム**:
  - **日付** (Date Picker)
    - 読み取り成功時: 自動入力
    - 読み取り失敗時: エラーメッセージ「読み取りができなかったので、入力してください」+ 必須入力フォーム
  - **店舗名** (Text Input)
    - 読み取り成功時: 自動入力（薄いグレーで「推測値」と表示）
    - 読み取り失敗時: エラーメッセージ「読み取りができなかったので、入力してください」+ 入力フォーム
  - **合計金額** (Number Input)
    - 読み取り成功（confidence: 'high'）: 自動入力 + 緑色のチェックマーク
    - 読み取り推測（confidence: 'low'）: 自動入力 + 黄色の警告「要確認」+ 数値のハイライト
    - 読み取り失敗（confidence: 'failed'）: エラーメッセージ「読み取りができなかったので、入力してください」+ 必須入力フォーム（赤枠）
  - **カテゴリ** (Select/Dropdown)
    - 自動分類されたカテゴリを初期値として表示
    - ユーザーが変更可能（変更時に学習データに保存）
  - **メモ** (Text Area) - 任意入力
- **アクション**: 保存、再撮影、キャンセル

**エラー表示のUI仕様**:
- エラーメッセージは各フィールドの直下に表示
- フォント色: `#ef4444` (赤)
- アイコン: ⚠️ または ❌
- エラー時は入力フィールドのボーダーを赤色 (`border-color: #ef4444`) に変更
- エラーフィールドは必須入力として扱い、保存時にバリデーション

## 5. 開発ロードマップ

### Phase 1: コア機能実装 (MVP)
- [ ] アプリの基本HTML/CSS構築
- [ ] Tesseract.jsの組み込みと動作確認
- [ ] 画像アップロード～文字表示の実装

### Phase 2: 解析ロジック強化
- [ ] `parser.js` の実装（日付・金額の自動抽出）
- [ ] 編集フォームの実装
- [ ] LocalStorageへの保存処理

### Phase 3: UI/UX改善・統合
- [ ] ダッシュボード画面の実装
- [ ] 週間棒グラフ表示 (Chart.js) + 予算ライン機能
- [ ] 自作カレンダー機能の実装（日付計算・グリッド生成）
- [ ] カテゴリ自動分類機能の実装 (`category-classifier.js`)
- [ ] カテゴリ学習機能の実装（ユーザー修正の保存）
- [ ] ポートフォリオ本体からのリンク作成

### Phase 4: 高度な機能（最後の調整）
- [ ] エディタ画面でのRaw Text ハイライト機能
  - Tesseract.jsのbbox情報を活用して画像上にテキスト位置を表示
  - 入力フィールドクリック時に該当箇所をハイライト

